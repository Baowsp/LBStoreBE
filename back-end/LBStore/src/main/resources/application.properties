spring.application.name=LBStore
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/lbstore1?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&zeroDateTimeBehavior=convertToNull}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:123456}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# PayOS Configuration - Thay bằng credentials thực từ dashboard.payos.vn
payos.client-id=${PAYOS_CLIENT_ID:6b972cc1-30e9-4910-8ceb-9d0649bf957f}
payos.api-key=${PAYOS_API_KEY:8528427a-325a-4949-a3e1-889180f4c14a}
payos.checksum-key=${PAYOS_CHECKSUM_KEY:7caf6ae1e2e8b84d949dab1cea20479083072974d30e120c6b62fdf8b5a91abf}
payos.return-url=${PAYOS_RETURN_URL:http://localhost:5173/checkout/success}
payos.cancel-url=${PAYOS_CANCEL-URL:http://localhost:5173/checkout/cancel}

# Gmail SMTP - Cấu hình gửi OTP
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:nhe6060@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:ielefgyimhfshyja}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.default-encoding=UTF-8

# Kích thước tối đa cho file ảnh upload
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
spring.servlet.multipart.location=./uploads

# Base URL của backend - dùng để tạo full URL cho ảnh upload
# Trên VPS/Server: set biến môi trường APP_BASE_URL=http://IP:8080 hoặc https://domain.com
app.base-url=${APP_BASE_URL:http://localhost:8080}